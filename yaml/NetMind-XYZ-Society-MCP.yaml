apiVersion: apps/v1
kind: Deployment
metadata:
  name: xyz_a2a_mcp
  namespace: xyz
  labels:
    app: xyz_a2a_mcp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: xyz_a2a_mcp
  template:
    metadata:
      name: xyz_a2a_mcp
      labels:
        app: xyz_a2a_mcp
      annotations:
        prometheus.io/scrape: "true"
        fluentd.io/log: "true"
    spec:
      containers:
        - name: xyz_a2a_mcp
          image: 891377017161.dkr.ecr.eu-west-2.amazonaws.com/xyz_a2a_mcp:${GIT_COMMIT_ID}
          ports:
            - containerPort: 10254
              protocol: TCP
          resources:
            requests:
              cpu: "500m"
              memory: "500Mi"
          readinessProbe:
            exec:
              command:
              - sh
              - -c
              - "nc -z localhost 10254"
            initialDelaySeconds: 5
            timeoutSeconds: 2
            periodSeconds: 10
            successThreshold: 1
            failureThreshold: 3
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                topologyKey: kubernetes.io/hostname
            - weight: 50
              podAffinityTerm:
                topologyKey: kubernetes.io/zone
      restartPolicy: Always

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 0
---
apiVersion: v1
kind: Service
metadata:
  name: xyz_a2a_mcp-service
spec:
  selector:
    app: xyz_a2a_mcp
  ports:
  - name: xyz_a2a_mcp-port-10254
    protocol: TCP
    port: 10254
    targetPort: 10254
